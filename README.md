# CHING'ENDE Album Launch — Static Website

Static GitHub Pages / Netlify deployment.

## Netlify
- Build command: leave empty
- Publish directory: `.`

The site loads `./assets/styles.css` and `./assets/app.js` directly. No Vite/Node build is required.
